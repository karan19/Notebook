import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Reverting standalone for standard Amplify SSR support
  // No specific output required for standard Amplify Hosting
};

export default nextConfig;
